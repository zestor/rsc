{
  "name": "automated-sourcing-review-app",
  "version": "0.1.0",
  "private": true,
  "description": "SQLite-backed review app for the Automated Sourcing workflow (scaffolded by recruitment/automated-sourcing).",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "seed": "node scripts/seed.js",
    "lint": "node --check server/index.js && node --check scripts/seed.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2",
    "express-rate-limit": "^8.5.2"
  },
  "engines": {
    "node": ">=18"
  }
}
