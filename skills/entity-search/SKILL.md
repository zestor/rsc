# Entity Search

- `entity-search/people-search` — Find people by name, role, company, education, skill, or location. Uses the `pplx search people` profile index with web-search fallback for coverage gaps.

To load a sub-skill: load_skill(name="entity-search/<sub-skill>")